/** 添加学生需拉取多月对局并重算 daily_game_stats，延长 Server Action 时限。 */
export const maxDuration = 300;

export default function NewPlayerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
